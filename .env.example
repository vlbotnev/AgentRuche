# .env.example

# MongoDB Credentials
MONGO_USER=admin
MONGO_PASS=password
MONGO_LOCAL_HOST=localhost
MONGO_LOCAL_PORT=27021
MONGO_DOCKER_HOST=mongodb
MONGO_DB_NAME=call_analyzer

# Minio Credentials
MINIO_ENDPOINT=localhost:9000
MINIO_USER=minioadmin
MINIO_PASS=miniopassword
MINIO_BUCKET=recordings

# Redis Config
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_QUEUE_NAME=call_processing_queue

# AI Provider API Key (e.g., Google Gemini)
GOOGLE_API_KEY="YOUR_API_KEY_HERE"

# URL the frontend uses to talk to the backend
BACKEND_API_URL=http://localhost:8000